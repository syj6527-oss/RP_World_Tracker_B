# PAW MAP v0.9.50 Secure Beta — 인수 메모

## v0.9.50 핵심 회귀 수정

- 장소 자동 감지·등록을 기본 동작으로 유지했습니다. 정상적인 좌표 불명 장소는 현재 장소 30~150m 반경의 안정적인 `≈ 추정 핀`으로 표시됩니다.
- 지도 검색창은 등록 장소를 먼저 검색하고, 결과가 없을 때 Enter를 누르면 Photon/OpenStreetMap 검색으로 이어집니다. 입력 중에는 외부 요청을 보내지 않습니다.
- `Room`, `Bedroom`, `Kitchen`, `방`, `침실` 같은 단독 내부 장소는 현재 상위 장소가 있을 때만 하위 장소로 자동 등록합니다. 상위 장소가 없으면 Photon에 보내지 않고 후보로 보류합니다.
- `Drift point` 등 감사·메타 로그를 이벤트 추출 전에 제외하고, 모바일 이벤트 카드 펼침·닫기를 수정했습니다.
- 왼쪽으로 돌아가던 지도 핀을 아래를 향하도록 수정했습니다.

## RP 경로·Google 링크·커뮤니티

- `RP 경로`는 등록된 두 장소 사이에 직선 경로·거리·예상 도보 시간을 표시합니다. 외부 경로 API를 쓰지 않고 DB·이동 기록·거리 관계에 저장하지 않는 세션 전용 표시입니다.
- Google 지도·Google 길찾기·Street View 링크는 기본 OFF이며 설정에서 켰 때만 버튼이 표시됩니다. 기능 코드는 삭제하지 않았습니다.
- 커뮤니티는 `기본 생성`과 `Overpass 주변 정보 보강 생성`을 선택할 수 있습니다. 보강 방식은 확정 좌표만 소수점 4자리로 반올림해 Overpass에 보내며 `≈ 추정 핀`에서는 차단됩니다.
- 두 커뮤니티 생성 방식 모두 선택한 SillyTavern 연결 프로필의 AI를 사용합니다. PAW MAP이 별도 카드나 결제 계정을 연결하지 않으며, 해당 공급자 계정의 토큰·크레딧·사용량이 차감됩니다.
- 연결 프로필로 전송하는 임시 프롬프트와 선택형 일반 채팅 지도 맥락에서 흔한 API 키·비밀 할당값, access/refresh/id token, Basic·Bearer 인증값, PEM 개인키 형태를 마스킹합니다. system 채팅과 프로필 프리셋·지시문은 유틸리티 생성 맥락에서 제외하며, 저장된 RP 원문과 백업 데이터는 임의로 바꾸지 않습니다.

## v0.9.49 핵심 자동 등록 복구

- v0.9.47에서 잘못 제거한 `장소 감지 → 자동 등록·이동`을 기본 동작으로 복구했습니다.
- 오탐 필터 강화는 유지합니다. 시스템 문구·대명사·문장 조각·사물 복합어 등 거부 대상은 자동 등록되지 않습니다.
- 새 장소에 실제 좌표가 없고 현재 장소에는 좌표가 있으면, 현재 장소 30~150m 반경에 `≈ 추정 핀`을 자동 생성합니다.
- 추정 핀은 로컬 계산만 사용하며 실제 주소·Street View 좌표로 취급하지 않습니다.
- 후보함은 자동 등록을 대체하는 강제 절차가 아니라 사용자가 선택할 수 있는 확인 모드로 돌렸습니다.
- 외부 AI·RP 공유·감지 장소명 Photon 전송은 계속 별도 동의가 필요합니다.

## v0.9.48 주소·모바일 팝업 회귀 수정

- 한글·영문 주소 검색이 모두 `결과 없음`으로 끝나던 Photon 언어 파라미터 오류를 수정했습니다.
- 지도를 직접 꾹 눌러 등록하거나 핀을 직접 옮기면 해당 RP 좌표의 주소를 자동 입력합니다. 감지된 채팅 장소명의 백그라운드 검색은 계속 기본 OFF입니다.
- 의심 장소 점검의 `선택 장소 삭제` 창을 모바일 화면의 하단과 안전 영역 안에 맞추고, 닫기 버튼·바깥 탭·Esc 닫기를 보강했습니다.
- 외부 AI는 새 결제 계정/카드를 연결하지 않으며, 사용자가 이미 선택한 SillyTavern 연결 프로필 공급자 계정의 토큰·크레딧·사용량을 씁니다. 실제 청구 방식은 그 기존 계정 설정을 따릅니다.

## 눈에 보이는 변경

- 상단 `🪧` 버튼과 숫자 배지로 장소 후보함을 엽니다.
- 후보 카드는 탐지 출처·신뢰도·판정 이유·일시적 근거를 보여주며, 이름 수정 후 기존 장소 연결/새 등록/이번만 무시/계속 무시를 선택할 수 있습니다.
- 설정과 데이터 메뉴에 `🧹 의심 장소 점검`을 추가했습니다. 항목은 기본 미선택이며, 사용자가 체크하고 재확인해야만 삭제됩니다.
- 같은 점검 화면에서 체크한 기존 핀의 좌표·주소만 초기화할 수 있습니다. 장소·이벤트·이동·약도 위치는 남습니다.
- `Google 지도 링크 표시`를 켰 때만 장소 상세와 모바일 바텀시트에 `Google에서 보기`, `Google 길찾기`, `Street View` 버튼이 나타납니다.

## 오탐 차단 경계

기본 자동 모드: `채팅 탐지 → 오탐 필터 → 영구 장소/이동`

선택 확인 모드: `채팅 탐지 → 메모리 후보 → 사용자 승인 → 영구 장소/이동`

확인 모드의 승인 전 후보는 IndexedDB와 백업에 들어가지 않습니다. 자동 모드에서 필터를 통과한 새 장소는 즉시 등록되며, 실제 좌표가 없으면 현재 장소 주변 추정 핀으로 표시됩니다. 좌표 전체 쌍 자동 연결은 계속 만들지 않습니다.

## Google 연동 범위

Google 데이터를 PAW MAP 내부로 가져오지 않습니다. 공식 [Maps URLs](https://developers.google.com/maps/documentation/urls/get-started) 형식으로 사용자가 누른 순간 새 탭을 여는 보조 기능만 제공합니다. API 키·SDK·Places/Routes API·Google Cloud 결제 계정은 사용하지 않습니다.

Google 길찾기는 저장된 출발지와 목적지를 모두 넣습니다. 출발지를 생략해 Google이 기기 위치를 대신 쓰는 동작을 허용하지 않습니다. 고정 HTTPS 호스트, 동작별 파라미터 허용목록, `noopener,noreferrer`를 적용했습니다.

## 확인한 항목

- 스크린샷 오탐 이름과 대명사/한 글자/문장 조각 거부
- `Crawl home`의 Home 연결 제안, `Club 앞`의 상대 위치 판정
- `병원` 안의 `원`, `상점` 안의 `점`, `bar of soap`, `office chair`, `club sandwich`, `office party` 부분 오탐 방지
- `left Club`/`클럽에서 나왔다`를 도착으로 뒤집지 않고 뒤에 명시된 실제 목적지만 선택하는지 확인
- 후보가 메모리에만 남고 계속 무시한 정규화 이름만 로컬 설정에 저장되는지 확인
- Google URL의 호스트·출발지/목적지·Street View 좌표·키/토큰/UTM 차단 확인
- 백업 비밀명 필드 제거, 자동 등록 필터 적용, 추정 핀의 반경·안정성·Street View 차단 확인
- 등록 장소 결과가 없는 Enter에서만 Photon이 호출되고, 부모 없는 `Room` 후보는 Photon·DB에 자동 승격되지 않으며 부모가 있으면 실제 하위 장소로 자동 등록되는지 확인
- `Drift point` 단독 메타 문구가 이벤트·AI 요청을 만들지 않고 모바일 카드가 펼침·닫기 되는지 확인
- RP 경로가 외부 요청·DB 저장 없이 동작하고 Google 링크가 기본 OFF인지 확인
- 커뮤니티 기본 생성은 Overpass를 호출하지 않고, 보강 생성은 확정 좌표에서만 Overpass를 1회 호출하는지 확인
- 모든 JavaScript 구문 검사와 `node tests/security-regression.mjs` 통과

SillyTavern 실제 화면의 장시간 모바일 상호작용과 각 외부 서비스 장애 상황은 자동 테스트로 완전히 재현하지 못했습니다. 설치 전 기존 데이터 백업을 보관하고, v0.9.50은 별도 베타로 먼저 확인하세요.
